{
  "name": "nx",
  "author": "lencx",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "./scripts/build",
    "watch": "./scripts/watch",
    "clean": "rimraf packages/**/{lib,esm}/"
  },
  "devDependencies": {
    "lerna": "^3.18.4",
    "rimraf": "^3.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
